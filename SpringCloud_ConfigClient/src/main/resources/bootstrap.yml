spring:
  application:
  	#对应远程git配置前缀名   根据name和profile指定远程git配置文件
    name: config-server
  cloud:
    config:
      #远程git分支	
      label: master
      #指定服务配置中心地址
      uri: http://localhost:8888/
      #对应远程git配置环境标识
      profile: dev
server:
  port: 8881