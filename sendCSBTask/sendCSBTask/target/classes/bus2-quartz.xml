<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- For times when you just need to invoke a method on a specific object -->
	<bean id="bus2JobDetail"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="bus2ScheduledJob" />
		<property name="targetMethod" value="bus2Job" />
		<property name="concurrent"   value="false"/>
	</bean>

	<!-- Run the job every 2 seconds with initial delay of 1 second -->
<bean id="bus2JobTrigger"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="bus2JobDetail" />
		<property name="cronExpression">
        <value>#{env['time.bus2']}</value>    <!-- 12点     0 00 00 ? * *    -->
    	</property>
	</bean>


	<!-- Scheduler factory bean to glue together jobDetails and triggers to 
		Configure Quartz Scheduler -->
	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="jobDetails">
			<list>
				<ref bean="bus2JobDetail" />
			</list>
		</property>
		<property name="triggers">
			<list>
				<ref bean="bus2JobTrigger" />
			</list>
		</property>
		<property name="taskExecutor" ref="taskExecutor" />
	</bean>
<bean id="bus2ScheduledJob" class="com.allinfinance.scheduler.Bus2ScheduledJob">
<property name="recordFileUploadService" ref="recordFileUploadService"/>
</bean>
<bean id="recordFileUploadService"  class="service.RecordFileUploadService">
</bean>
</beans>